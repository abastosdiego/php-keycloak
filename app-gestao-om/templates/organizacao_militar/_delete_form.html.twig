<form method="post" action="{{ path('app_organizacao_militar_delete', {'id': organizacao_militar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organizacao_militar.id) }}">
    <button class="btn">Delete</button>
</form>
